import { Meta, Story, Canvas, Props } from "@storybook/blocks";

import { action } from '@storybook/addon-actions';
import { ViewPort, BottomResizable, TopResizable, RightResizable, LeftResizable } from "../../";
import { green, description, lorem } from "../Utils";
import { CommonHeader, PropsTable, StandardProps, AnchoredProps, ResizableProps } from "../Utils";
import * as ResizablePixelStories from './Resizable.Pixel.stories';

<CommonHeader />

<Meta of={ResizablePixelStories} />

## Resizable

Anchored resizable spaces by pixel size.

### Bottom resizable

<Canvas of={ResizablePixelStories.Bottom} />

### Top resizable

<Canvas of={ResizablePixelStories.Top} />

### Left resizable

<Canvas of={ResizablePixelStories.Left} />

### Right resizable

<Canvas of={ResizablePixelStories.Right} />

### Properties

<ResizablePixelStories.Properties />
